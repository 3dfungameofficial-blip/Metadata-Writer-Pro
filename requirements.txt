ttkbootstrap>=8.1.0
pandas>=2.0.0
pyexiv2>=2.12.0
imageio-ffmpeg>=0.5.0
darkdetect>=0.8.0
pytest>=8.0.0
# Optional (gracefully skipped when absent):
# tkinterdnd2>=0.1.0   # drag-and-drop of CSV/folders into the window
